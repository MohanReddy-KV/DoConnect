# DoConnect

> HCLTech training capstone project built with ASP.NET MVC, C#, and SQL Server.

## Overview

DoConnect is a web application created as part of my HCLTech training capstone. It helped me practice building a structured .NET web application using the MVC pattern, connecting application logic to a SQL Server database, and working with server-rendered views.

This repository is preserved as part of my learning and development journey.

## Technology Stack

- ASP.NET MVC (.NET Framework)
- C#
- SQL Server
- HTML, CSS, and JavaScript
- Visual Studio

## Project Structure

```text
DoConnect/
├── Controllers/       # Request handling and application flow
├── Models/            # Application and data models
├── Views/             # Server-rendered user interface
├── Content/           # Stylesheets and static content
├── Scripts/           # Client-side scripts
├── App_Start/         # Application configuration
└── DoConnect.csproj   # Project file
```

The repository also includes:

- `DoConnect.sln` — Visual Studio solution
- `Do connect mini project 2.sql` — SQL Server database script
- `Do connect Project ScreenShots/` — Project screenshots

## Running the Project

1. Clone the repository.
2. Open `DoConnect.sln` in Visual Studio.
3. Run the included SQL script in SQL Server.
4. Update the database connection string in the application configuration if needed.
5. Build and run the project from Visual Studio.

> This was created during training and may need environment-specific configuration updates before running locally.

## What I Learned

- Applying the MVC architecture in a .NET web application
- Organizing controllers, models, and views
- Working with C# application logic
- Connecting an application to SQL Server
- Using Git and GitHub to manage project code

## Screenshots

Project screenshots are available in the [`Do connect Project ScreenShots`](./Do%20connect%20Project%20ScreenShots) folder.

## Future Direction

I am continuing to build toward modern full-stack development with **ASP.NET Core, React, Azure, and AI-powered application features**.
